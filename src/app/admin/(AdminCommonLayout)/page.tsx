import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Chair Source | Dashboard",
  description:
    "This is admin dashboard page",
  // other metadata
};

const AdminPage = () => {
  return <div>AdminPage</div>;
};

export default AdminPage;
