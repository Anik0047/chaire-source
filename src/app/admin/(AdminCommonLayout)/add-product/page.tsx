import React from "react";
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: "Dashboard | Add Product",
  description:
    "This is product add for admin",
  // other metadata
};
const AddProduct = () => {
  return <div>AddProduct</div>;
};

export default AddProduct;
